import 'package:flutter/material.dart';

class AppColors {
  // --- FURNITURE UX DESIGN SYSTEM ---
  // A palette designed for high-end commerce: High contrast, clean surfaces, and professional trust.

  static const primary = Color(0xFFFFFFFF);     // Pure White - The ultimate canvas for furniture images
  static const secondary = Color(0xFFF9FAFB);   // Soft Pearl Gray - For subtle depth in cards and sections
  
  // Brand & High-Contrast Elements
  static const furnitureBlue = Color(0xFF1E3A8A); // Deep Midnight Blue - Represents reliability and architectural depth
  static const headerLine = Color(0xFF111827);    // Deepest Charcoal - For sharp, readable typography
  static const bodyLine = Color(0xFF4B5563);      // Slate Gray - For professional secondary text
  
  // UX Action Colors
  static const accentButton = Color(0xFF1E3A8A);  // Primary Action - Consistent with Brand Blue
  static const divider = Color(0xFFE5E7EB);       // Modern, light divider for clean spacing
  
  // Semantic Commerce Colors
  static const saleRed = Color(0xFFDC2626);       // Urgent but professional Red for discounts (Furniture Sales)
  static const successGreen = Color(0xFF059669);  // Trust-building Green for checkout/success
  static const ratingGold = Color(0xFFF59E0B);    // Premium Gold for star ratings
  
  // Interactive Overlays
  static const hoverGray = Color(0xFFF3F4F6);
}
